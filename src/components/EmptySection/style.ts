import styled from "@emotion/styled"

export const Wrapper = styled.div`
  .section-empty {
    img {
      width: 100px;
      object-fit: contains;
    }

    .illustration {
      opacity: 0.4;
    }

    .empty-title {
      font-weight: 400;
      font-size: 24px;
      line-height: 34px;
    }

    .empty-desc {
      font-weight: 400;
      font-size: 18x;
      line-height: 18px;
      color: #667085;
    }

    .empty-button {
      width: min-width;
      border: none;
    }
  }
`
